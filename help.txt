/*==================== СПЕЦИФИКАТОРЫ ФОРМАТА ============================//
				<-- %d или &i - целые числа со знаком	 (int) -->
				<-- %c - одиночный символ				 (char) -->
				<-- %f - числа с плавающей точкой		 (float) -->
				<-- %lf -								 (double) -->
				<-- %s - для строк						 (string) -->
 =========================================================================
*/

/*============================== ФУНКЦИИ ==============================//
// тип ф-ии _ имя(что принимает) {тело функции, что выполняет}
// void - ничего не возвращают, только выполняют действия в теле функции.
// прототип функции 
=========================================================================
*/

/*==================== СПЕЦИФИКАТОРЫ ФОРМАТА ============================//
				<-- %d или &i - целые числа со знаком	 (int) -->
				<-- %c - одиночный символ				 (char) -->
				<-- %f - числа с плавающей точкой		 (float) -->
				<-- %lf -								 (double) -->
				<-- %s - для строк						 (string) -->
 =========================================================================
*/

#include <stdlib.h>  // обязательно для malloc и free

void* malloc(size_t size);  // выделяет size байт памяти
void free(void* ptr);       // освобождает память

* - означает, что это указатель.

malloc(Количество_элементов × sizeof(Тип))

Примеры:
malloc(10 * sizeof(char))
10 × 1 = 10 байт
malloc(10 * sizeof(int))
10 × 4 = 40 байт
malloc(10 * sizeof(double))
10 × 8 = 80 байт
malloc(5 * sizeof(long long))
5 × 8 = 40 байт

// побочная диагональ
sum_poboch = sum_poboch + matrix[i][3 - i - 1]; 3 - размер матрицы;

// Внутренний цикл, строка на столбец.
for (int a = 0; a < m2; a++) {
				squad_matrix[i][j] = squad_matrix[i][j] + matrix_2[i][a] * matrix_2[a][j];

